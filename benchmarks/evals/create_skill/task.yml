before_run:
  - cargo build
run:
  command: target/debug/forge -p {{prompt}}
  parallelism: 10
validations:
  - name: "Check success message"
    type: matches_regex
    regex: "Skill created successfully"
sources:
  - csv: create_skill_tasks.csv
