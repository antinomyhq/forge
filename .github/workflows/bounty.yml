# -------------------------------------------------------------------
# ------------------------------- WARNING ---------------------------
# -------------------------------------------------------------------
#
# This file was automatically generated by gh-workflows using the
# gh-workflow-gen bin. You should add and commit this file to your
# git repository. **DO NOT EDIT THIS FILE BY HAND!** Any manual changes
# will be lost if the file is regenerated.
#
# To make modifications, update your `build.rs` configuration to adjust
# the workflow description as needed, then regenerate this file to apply
# those changes.
#
# -------------------------------------------------------------------
# ----------------------------- END WARNING -------------------------
# -------------------------------------------------------------------

name: Bounty Management
'on':
  issues:
    types:
    - assigned
    - unassigned
    - labeled
    - unlabeled
  pull_request:
    types:
    - opened
    - edited
    - reopened
  pull_request_target:
    types:
    - closed
permissions:
  issues: write
  pull-requests: write
jobs:
  propagate-bounty-label:
    name: Propagate bounty label to PR
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Parse event into intent
      run: npx tsx .github/scripts/bounty/parse-propagate-label.ts --pr ${{ github.event.pull_request.number }} > /tmp/intent.json
    - name: Plan — diff desired vs current labels
      run: INTENT_FILE=/tmp/intent.json npx tsx .github/scripts/bounty/plan.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }} > /tmp/plan.json
    - name: Execute batch label mutations
      run: PLAN_FILE=/tmp/plan.json npx tsx .github/scripts/bounty/execute.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }}
  sync-claimed-label:
    name: Sync bounty claimed label on assignment
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Parse event into intent
      run: npx tsx .github/scripts/bounty/parse-sync-claimed.ts --issue ${{ github.event.issue.number }} > /tmp/intent.json
    - name: Plan — diff desired vs current labels
      run: INTENT_FILE=/tmp/intent.json npx tsx .github/scripts/bounty/plan.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }} > /tmp/plan.json
    - name: Execute batch label mutations
      run: PLAN_FILE=/tmp/plan.json npx tsx .github/scripts/bounty/execute.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }}
  sync-generic-bounty-label:
    name: Sync generic bounty label on value label change
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Parse event into intent
      run: npx tsx .github/scripts/bounty/parse-sync-generic-bounty.ts --issue ${{ github.event.issue.number }} > /tmp/intent.json
    - name: Plan — diff desired vs current labels
      run: INTENT_FILE=/tmp/intent.json npx tsx .github/scripts/bounty/plan.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }} > /tmp/plan.json
    - name: Execute batch label mutations
      run: PLAN_FILE=/tmp/plan.json npx tsx .github/scripts/bounty/execute.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }}
  mark-bounty-rewarded:
    name: Mark bounty as rewarded on PR merge
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Parse event into intent
      run: npx tsx .github/scripts/bounty/parse-mark-rewarded.ts --pr ${{ github.event.pull_request.number }} > /tmp/intent.json
    - name: Plan — diff desired vs current labels
      run: INTENT_FILE=/tmp/intent.json npx tsx .github/scripts/bounty/plan.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }} > /tmp/plan.json
    - name: Execute batch label mutations
      run: PLAN_FILE=/tmp/plan.json npx tsx .github/scripts/bounty/execute.ts --repo ${{ github.repository }} --token ${{ secrets.GITHUB_TOKEN }}
