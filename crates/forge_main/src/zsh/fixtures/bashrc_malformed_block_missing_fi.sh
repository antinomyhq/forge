# My bashrc
export PATH=$PATH:/usr/local/bin

# Added by zsh installer
if [ -t 0 ] && [ -x "/usr/bin/zsh" ]; then
  export SHELL="/usr/bin/zsh"

# Content after incomplete block (will be lost)
alias ll='ls -la'
