{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); if echo \"$input\" | grep -q 'rm -rf /'; then echo 'BLOCKED: rm -rf / is not allowed' >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
