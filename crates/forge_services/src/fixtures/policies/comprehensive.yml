policies:
  - permission: allow
    rule:
      read: "**/*.{rs,js,ts,py}"
  - permission: confirm
    rule:
      write: "src/**/*"
  - permission: deny
    rule:
      command: "rm -rf /*"
  - permission: allow
    rule:
      command: "cargo*"