---
name: setup-zsh
description: Install and configure the Forge ZSH plugin with all dependencies
---

Load the `setup-zsh` skill and follow its workflow to install and configure the Forge ZSH plugin with all dependencies (fzf, fd, bat, zsh-syntax-highlighting).
